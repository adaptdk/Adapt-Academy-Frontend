import { createAction } from 'redux-actions';

// Constants.
import { LIST_SET_ACTIVE } from '../../contants/list';

const listSetActive = createAction(LIST_SET_ACTIVE);

export default listSetActive;
